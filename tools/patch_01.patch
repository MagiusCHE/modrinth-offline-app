diff --git a/packages/app-lib/src/api/minecraft_auth.rs b/packages/app-lib/src/api/minecraft_auth.rs
index 931b8e75b..7c3b9560e 100644
--- a/packages/app-lib/src/api/minecraft_auth.rs
+++ b/packages/app-lib/src/api/minecraft_auth.rs
@@ -38,6 +38,13 @@ pub async fn finish_login(
 
 #[tracing::instrument]
 pub async fn get_default_user() -> crate::Result<Option<uuid::Uuid>> {
+    if let Ok(username) = std::env::var("MODRINTH_OFFLINE_USERNAME") {
+        if !username.is_empty() {
+            let offline_creds = Credentials::offline(username);
+            return Ok(Some(offline_creds.offline_profile.id));
+        }
+    }
+
     let state = State::get().await?;
     let user = Credentials::get_active(&state.pool).await?;
     Ok(user.map(|user| user.offline_profile.id))
@@ -84,6 +91,12 @@ pub async fn remove_user(uuid: uuid::Uuid) -> crate::Result<()> {
 /// Get a copy of the list of all user credentials
 #[tracing::instrument]
 pub async fn users() -> crate::Result<Vec<Credentials>> {
+    if let Ok(username) = std::env::var("MODRINTH_OFFLINE_USERNAME") {
+        if !username.is_empty() {
+            return Ok(vec![Credentials::offline(username)]);
+        }
+    }
+
     let state = State::get().await?;
     let users = Credentials::get_all(&state.pool).await?;
     Ok(users.into_iter().map(|x| x.1).collect())
diff --git a/packages/app-lib/src/api/minecraft_skins.rs b/packages/app-lib/src/api/minecraft_skins.rs
index dc4373e67..94f08b3ea 100644
--- a/packages/app-lib/src/api/minecraft_skins.rs
+++ b/packages/app-lib/src/api/minecraft_skins.rs
@@ -83,6 +83,13 @@ mod assets {
 
 mod png_util;
 
+fn is_offline_mode() -> bool {
+    match std::env::var("MODRINTH_OFFLINE_USERNAME") {
+        Ok(username) => !username.is_empty(),
+        Err(_) => false,
+    }
+}
+
 const SKIN_CHANGE_DEBOUNCE: Duration = Duration::from_secs(10);
 
 static PENDING_SKIN_CHANGE: LazyLock<Mutex<PendingSkinChangeState>> =
@@ -264,6 +271,10 @@ pub enum UrlOrBlob {
 /// Only one cape can be equipped.
 #[tracing::instrument]
 pub async fn get_available_capes() -> crate::Result<Vec<Cape>> {
+    if is_offline_mode() {
+        return Ok(Vec::new());
+    }
+
     let state = State::get().await?;
 
     let selected_credentials = Credentials::get_default_credential(&state.pool)
@@ -304,6 +315,10 @@ pub async fn get_available_capes() -> crate::Result<Vec<Cape>> {
 /// Exactly one returned skin is marked as equipped.
 #[tracing::instrument]
 pub async fn get_available_skins() -> crate::Result<Vec<Skin>> {
+    if is_offline_mode() {
+        return Ok(Vec::new());
+    }
+
     let state = State::get().await?;
 
     let selected_credentials = Credentials::get_default_credential(&state.pool)
diff --git a/packages/app-lib/src/state/minecraft_auth.rs b/packages/app-lib/src/state/minecraft_auth.rs
index c021a239d..ca8a57baf 100644
--- a/packages/app-lib/src/state/minecraft_auth.rs
+++ b/packages/app-lib/src/state/minecraft_auth.rs
@@ -447,6 +447,16 @@ impl Credentials {
     pub async fn get_default_credential(
         exec: impl sqlx::Executor<'_, Database = sqlx::Sqlite> + Copy,
     ) -> crate::Result<Option<Credentials>> {
+        if let Ok(username) = std::env::var("MODRINTH_OFFLINE_USERNAME") {
+            if !username.is_empty() {
+                tracing::info!(
+                    "Using offline mode with username: {}",
+                    username
+                );
+                return Ok(Some(Credentials::offline(username)));
+            }
+        }
+
         let credentials = Self::get_active(exec).await?;
 
         if let Some(mut creds) = credentials {
@@ -474,6 +484,32 @@ impl Credentials {
         }
     }
 
+    /// Creates fake offline credentials for offline-mode servers.
+    pub fn offline(username: String) -> Self {
+        let offline_uuid = {
+            let hash = sha2::Sha256::digest(
+                format!("OfflinePlayer:{}", username).as_bytes(),
+            );
+            let mut bytes = [0u8; 16];
+            bytes.copy_from_slice(&hash[..16]);
+            bytes[6] = (bytes[6] & 0x0f) | 0x40;
+            bytes[8] = (bytes[8] & 0x3f) | 0x80;
+            Uuid::from_bytes(bytes)
+        };
+
+        Self {
+            offline_profile: MinecraftProfile {
+                id: offline_uuid,
+                name: username,
+                ..MinecraftProfile::default()
+            },
+            access_token: "offline_mode_token".to_string(),
+            refresh_token: String::new(),
+            expires: Utc::now() + Duration::days(365 * 100),
+            active: true,
+        }
+    }
+
     /// Fetches the currently selected credentials from the database, attempting
     /// to refresh them if they are expired.
     pub async fn get_active(
diff --git a/turbo.jsonc b/turbo.jsonc
index f1ad4c6ec..e3dda5160 100644
--- a/turbo.jsonc
+++ b/turbo.jsonc
@@ -77,7 +77,14 @@
 			"persistent": true,
 			"inputs": ["$TURBO_DEFAULT$", ".env*"],
 			"env": ["CARGO_*", "RUST_*", "RUSTFLAGS", "FORCE_COLOR", "NEXTEST_*"],
-			"passThroughEnv": ["DISPLAY", "WEBKIT_DISABLE_DMABUF_RENDERER", "JAVA_HOME"]
+			"passThroughEnv": [
+				"DISPLAY",
+				"GDK_BACKEND",
+				"JAVA_HOME",
+				"MODRINTH_OFFLINE_USERNAME",
+				"WEBKIT_DISABLE_DMABUF_RENDERER",
+				"WINIT_UNIX_BACKEND"
+			]
 		},
 		"test": {
 			"env": [
